import { MapComponentProps } from './types';
import { BaseballMap } from './index';

const BaseballMapComponent = (props: MapComponentProps) => {
  return <BaseballMap {...props} />;
};

export default BaseballMapComponent;
