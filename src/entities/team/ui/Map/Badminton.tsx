import { MapComponentProps } from './types';
import { BadmintonMap } from './index';

const BadmintonMapComponent = (props: MapComponentProps) => {
  return <BadmintonMap {...props} />;
};

export default BadmintonMapComponent;
