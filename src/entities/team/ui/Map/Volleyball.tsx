import { MapComponentProps } from './types';
import { VolleyballMap } from './index';

const VolleyballMapComponent = (props: MapComponentProps) => {
  return <VolleyballMap {...props} />;
};

export default VolleyballMapComponent;
